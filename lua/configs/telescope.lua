return {
    defaults = {
        selection_caret = " ",
        entry_prefix = " ",
        file_ignore_patterns = { ".gitlab%-ci%-local/", ".cache", "%.o", "%.a", "%.out", "%.class",
		"%.pdf", "%.mkv", "%.mp4", "%.zip" },
    },
}